This directory contains native helper binaries packaged with AFA.

For macOS builds, run:

  npm run build:helper:macos

That builds the Swift Core Audio helper and copies it here as:

  resources/native/AFAAudioHelper
